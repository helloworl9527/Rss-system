-- ============================================================
-- 004 来源主页 URL
--
-- 抓取端点多为 RSS 地址（如 linuxdorss.longpink.com/top.xml），
-- 不适合给人点击。site_url 是人类可访问的主页，
-- 邮件里的渠道名指向此处。
-- ============================================================
ALTER TABLE sources ADD COLUMN site_url TEXT;
